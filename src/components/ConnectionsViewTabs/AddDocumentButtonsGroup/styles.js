import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    button: {
        backgroundColor: 'black',
        color: 'white',
        borderRadius: 25,
        width: '30%',
    },
});
